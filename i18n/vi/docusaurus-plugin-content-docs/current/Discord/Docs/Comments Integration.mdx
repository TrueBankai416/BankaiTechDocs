---
title: Tích hợp bình luận
description: Hướng dẫn đầy đủ về cách tích hợp hệ thống bình luận hai chiều Discord với Docusaurus, bao gồm thiết lập, cấu hình và khắc phục sự cố.
image: /img/social/discord-social.jpg
sidebar_position: 2
---

# Tích hợp bình luận Discord

Trang tài liệu này bao gồm **tích hợp bình luận Discord hai chiều** cho phép khách truy cập gửi bình luận đến máy chủ Discord của bạn VÀ hiển thị phản hồi Discord trên trang web cùng với hệ thống bình luận Giscus dựa trên GitHub hiện có.

## Đặc trưng

- **Giao tiếp hai chiều**: Bình luận trên trang web → Discord, Trả lời trên Discord → Trang web
- **Hệ thống bình luận kép**: Cả hệ thống bình luận Giscus (dựa trên GitHub) và Discord đều hoạt động đồng thời.
- **Nhúng bình luận Discord đa dạng**: Bình luận được hiển thị dưới dạng nội dung nhúng được định dạng đầy đủ với ngữ cảnh toàn trang.
- **Quản lý luồng thảo luận**: Tự động tạo luồng thảo luận để sắp xếp các câu trả lời một cách có hệ thống.
- **Cập nhật theo thời gian thực**: Trang web tự động hiển thị các phản hồi trên Discord.
- **Ngữ cảnh toàn trang**: Các bình luận bao gồm tiêu đề trang, URL và ngữ cảnh điều hướng.
- **Ảnh đại diện người dùng**: Các tin nhắn trả lời trên Discord hiển thị ảnh đại diện và tên người dùng.
- **Thiết kế tương thích**: Hoạt động tốt trên cả máy tính để bàn và thiết bị di động.

## Hướng dẫn cài đặt

### 1. Create a Discord Bot

1. Truy cập [Cổng thông tin nhà phát triển Discord](https://discord.com/developers/applications)
2. Tạo ứng dụng mới
3. Vào mục "Bot" và tạo một bot.
4. Sao chép mã thông báo của bot
5. Trong mục "Ý định cổng đặc quyền", hãy bật:
   - **Mục đích nội dung tin nhắn**
   - **Ý định của các thành viên máy chủ** (tùy chọn)

### 2. Configure Bot Permissions

Bot của bạn cần các quyền sau:

- Đọc tin nhắn
- Gửi tin nhắn
- Tạo chủ đề thảo luận công khai
- Gửi tin nhắn theo nhóm
- Đọc lịch sử tin nhắn
- Sử dụng lệnh gạch chéo (tùy chọn)

### 3. Invite Bot to Server

1. Truy cập OAuth2 > Trình tạo URL
2. Chọn phạm vi "bot"
3. Chọn các quyền được liệt kê ở trên.
4. Sử dụng URL được tạo để mời bot vào máy chủ của bạn.

### 4. Configure Environment Variables

#### Giao diện người dùng (.env)

```env
# Cấu hình Bot Discord
DISCORD_BOT_TOKEN=mã_bot_của_bạn_ở_đây
DISCORD_CHANNEL_ID=ID_kênh_của_bạn_ở_đây

# Cấu hình API
REACT_APP_API_URL=http://localhost:3001

# Cấu hình ngữ cảnh Discord
DISCORD_CONTEXT_LEVEL=đầy_đủ
DISCORD_SHOW_BREADCRUMBS=đúng
DISCORD_SHOW_SECTION_INFO=đúng
DISCORD_SITE_NAME=Tên_trang_của_bạn
```

#### Máy chủ phụ trợ (server/.env)

```env
# Cấu hình Bot Discord
DISCORD_BOT_TOKEN=your_bot_token_here
DISCORD_CHANNEL_ID=your_channel_id_here

# Cấu hình API
PORT=3001

# Cấu hình ngữ cảnh Discord
DISCORD_CONTEXT_LEVEL=full
DISCORD_SHOW_PAGE_CONTEXT=true
DISCORD_SHOW_BREADCRUMBS=true
DISCORD_SHOW_SECTION_INFO=true
DISCORD_SHOW_PAGE_PATH=false
DISCORD_SHOW_TIMESTAMP=true
DISCORD_SHOW_AUTHOR_FIELD=true
DISCORD_CREATE_THREADS=true
DISCORD_THREAD_DURATION=1440
DISCORD_SITE_NAME=Tên trang web của bạn
```

:::Cảnh báo Lưu ý về bảo mật

- Không bao giờ đưa các tệp `.env` vào hệ thống quản lý phiên bản.
- Sử dụng biến môi trường trong môi trường sản xuất
- Hãy bảo mật mã thông báo bot của bạn
  :::

### 5. Install Server Dependencies

```bash
cd server
npm install
```

### 6. Start the Backend Server

```bash
# Phát triển
npm run dev

# Sản xuất
npm start
```

### 7. Deploy Configuration

Đối với việc triển khai sản phẩm, hãy thiết lập các biến môi trường trong nền tảng lưu trữ của bạn:

- **Giao diện người dùng**: Đặt `REACT_APP_API_URL` thành URL API dành cho môi trường sản xuất của bạn.
- **Phần Backend**: Triển khai thư mục máy chủ với mã thông báo bot và ID kênh.
- **Cơ sở dữ liệu**: Cơ sở dữ liệu SQLite sẽ được tạo tự động.
- **Ngữ cảnh**: Cấu hình cấp độ ngữ cảnh và các tùy chọn của Discord theo sở thích của bạn.

## Cách thức hoạt động

### Dòng chảy hai chiều hoàn chỉnh

1. **Bình luận trên trang web**: Người dùng gửi bình luận thông qua biểu mẫu trên trang web.
2. **Tin nhắn Discord**: Bot gửi nội dung nhúng được định dạng đầy đủ đến kênh Discord.
3. **Tạo chủ đề**: Bot tự động tạo một chủ đề để sắp xếp các câu trả lời.
4. **Trả lời trên Discord**: Người dùng Discord trả lời trong luồng thảo luận hoặc trả lời tin nhắn.
5. **Lưu trữ cơ sở dữ liệu**: Bot lưu trữ tất cả bình luận và phản hồi trong cơ sở dữ liệu SQLite.
6. **Hiển thị trên trang web**: Giao diện người dùng thực hiện khảo sát API mỗi 30 giây và hiển thị phản hồi từ Discord.

### Định dạng tin nhắn Discord

Các nội dung nhúng Discord bao gồm:

- **Title**: "💬 New Comment: \{Page Title}"
- **Mô tả**: Nội dung bình luận thực tế
- **Tác giả**: Tên người bình luận
- **URL**: Liên kết có thể nhấp chuột dẫn đến trang cụ thể
- **Trường Trang**: Hiển thị tiêu đề trang kèm liên kết có thể nhấp chuột
- **Thời điểm**: Thời điểm bình luận được đăng tải
- **Chủ đề**: Chủ đề tự động tạo cho các câu trả lời

### Hiển thị trên trang web

Trang web hiển thị:

- **Bình luận gốc**: Các bình luận được gửi qua biểu mẫu trên trang web
- **Phản hồi trên Discord**: Các phản hồi từ người dùng Discord có ảnh đại diện và tên người dùng.
- **Cập nhật theo thời gian thực**: Tự động kiểm tra phản hồi mới
- **Hội thoại theo luồng**: Cấu trúc bình luận → trả lời được sắp xếp hợp lý.

## Cấu hình ngữ cảnh

### Các cấp độ ngữ cảnh

Tùy chỉnh mức độ hiển thị ngữ cảnh trong tin nhắn Discord:

#### `DISCORD_CONTEXT_LEVEL=minimal`

- **Hiển thị**: Chỉ tiêu đề trang và nội dung bình luận
- **Trường hợp sử dụng**: Các tin nhắn ngắn gọn, đơn giản, không kèm theo ngữ cảnh bổ sung.

#### `DISCORD_CONTEXT_LEVEL=basic`

- **Hiển thị**: Tiêu đề trang, nội dung bình luận và URL trang có thể nhấp chuột
- **Trường hợp sử dụng**: Bối cảnh trung bình với thông tin thiết yếu

#### `DISCORD_CONTEXT_LEVEL=full` (mặc định)

- **Hiển thị**: Tất cả thông tin ngữ cảnh bao gồm đường dẫn điều hướng, các phần, dấu thời gian.
- **Trường hợp sử dụng**: Cung cấp đầy đủ ngữ cảnh cho các cuộc thảo luận chi tiết

#### `DISCORD_CONTEXT_LEVEL=custom`

- **Hiển thị**: Chỉ các phần tử ngữ cảnh mà bạn bật riêng lẻ
- **Trường hợp sử dụng**: Kiểm soát chi tiết những gì hiển thị.

### Kiểm soát ngữ cảnh cá nhân

Khi sử dụng `DISCORD_CONTEXT_LEVEL=custom`, bạn có thể kiểm soát từng phần tử:

```env
DISCORD_SHOW_PAGE_CONTEXT=true # Hiển thị tiêu đề trang và URL
DISCORD_SHOW_BREADCRUMBS=true # Hiển thị đường dẫn điều hướng
DISCORD_SHOW_SECTION_INFO=true # Hiển thị phần chính (Docker, Nextcloud, v.v.)
DISCORD_SHOW_PAGE_PATH=false # Hiển thị đường dẫn URL đầy đủ
DISCORD_SHOW_TIMESTAMP=true # Hiển thị thời gian đăng bình luận
DISCORD_SHOW_AUTHOR_FIELD=true # Hiển thị tên người bình luận dưới dạng tác giả nhúng
```

### Cấu hình luồng

```env
DISCORD_CREATE_THREADS=true # Tạo luồng thảo luận để trả lời
DISCORD_THREAD_DURATION=1440 # Thời gian tự động lưu trữ luồng thảo luận (phút)
DISCORD_SITE_NAME=Tên Trang Web Của Bạn # Tên trang web trong chân trang
```

### Ví dụ về kết quả đầu ra theo ngữ cảnh

**Ngữ cảnh tối thiểu:**

```
Hướng dẫn cài đặt Docker
"Hướng dẫn này thực sự hữu ích! Tôi phải xử lý xung đột cổng như thế nào?"
```

**Bối cảnh cơ bản:**

```
💬 Hướng dẫn cài đặt Docker
"Hướng dẫn này thực sự hữu ích! Tôi phải xử lý xung đột cổng như thế nào?"
[Liên kết đến trang]
```

**Toàn bộ ngữ cảnh:**

```
💬 Bình luận mới: Hướng dẫn cài đặt Docker
👤 John Doe
"Hướng dẫn này thực sự hữu ích! Tôi phải xử lý xung đột cổng như thế nào?"

📄 Trang: Hướng dẫn cài đặt Docker
📍 Điều hướng: Docker > Tài liệu > Cài đặt Docker
📂 Mục: Docker
🔗 Đường dẫn: /Docker/Docs/Installing%20Ddocker/Docker%20Engine

Tên trang web của bạn • Hệ thống bình luận
```

## Tùy chỉnh

### Tạo kiểu tóc

The Discord comments component uses CSS modules. You can customize the appearance by modifying:

```
src/components/DiscordComments/styles.module.css
```

### Định dạng tin nhắn

Để tùy chỉnh định dạng nhúng Discord, hãy chỉnh sửa đối tượng `discordMessage` trong:

```
src/components/DiscordComments/index.tsx
```

### Vị trí linh kiện

The Discord comments appear below the Giscus comments by default. To change this, modify:

```
src/theme/DocItem/Layout/index.js
```

## Khắc phục sự cố

### Bot không phản hồi

1. **Kiểm tra mã token của bot**: Đảm bảo `DISCORD_BOT_TOKEN` chính xác.
2. **Kiểm tra quyền hạn**: Hãy chắc chắn rằng bot có các quyền cần thiết trong kênh.
3. **Kiểm tra ID kênh**: Đảm bảo `DISCORD_CHANNEL_ID` chính xác.
4. **Bot trong máy chủ**: Xác minh xem bot có thực sự đang ở trong máy chủ Discord của bạn hay không.

### Bình luận không hiển thị

1. **Máy chủ đang chạy**: Kiểm tra xem máy chủ phụ trợ có đang chạy trên đúng cổng hay không.
2. **URL API**: Hãy xác minh `REACT_APP_API_URL` trỏ đến máy chủ phụ trợ của bạn.
3. **Cơ sở dữ liệu**: Kiểm tra xem cơ sở dữ liệu SQLite có được tạo trong thư mục máy chủ hay không.
4. **CORS**: Hãy đảm bảo CORS đã được cấu hình cho tên miền giao diện người dùng của bạn.

### Phản hồi trên Discord không hiển thị

1. **Mục đích nội dung tin nhắn**: Hãy đảm bảo tùy chọn này được bật trong Cổng thông tin nhà phát triển Discord.
2. **Quyền hạn của bot**: Xác minh bot có thể đọc tin nhắn trong kênh.
3. **Kết nối cơ sở dữ liệu**: Kiểm tra nhật ký máy chủ để tìm lỗi cơ sở dữ liệu.
4. **Khảo sát**: Giao diện người dùng thực hiện khảo sát cứ sau 30 giây - phản hồi có thể mất một chút thời gian để hiển thị.

### Biến môi trường không hoạt động

- **Phát triển**: Khởi động lại cả máy chủ frontend và backend.
- **Môi trường sản xuất**: Triển khai lại cả giao diện người dùng (frontend) và máy chủ (backend) với các biến môi trường chính xác.
- **Tiền tố**: Chỉ sử dụng tiền tố `REACT_APP_` cho các biến frontend.

### Các vấn đề về cơ sở dữ liệu

1. **Quyền truy cập tập tin**: Đảm bảo máy chủ có quyền ghi vào thư mục cơ sở dữ liệu.
2. **Dung lượng ổ đĩa**: Kiểm tra dung lượng ổ đĩa còn trống cho cơ sở dữ liệu SQLite.
3. **Nhật ký máy chủ**: Kiểm tra bảng điều khiển máy chủ để tìm lỗi kết nối cơ sở dữ liệu.

## Các vấn đề liên quan đến an ninh

- **Mã thông báo Bot**: Hãy giữ bí mật mã thông báo bot của bạn và không bao giờ đưa nó vào hệ thống quản lý phiên bản.
- **Giới hạn số lượng yêu cầu**: Discord có giới hạn số lượng yêu cầu đối với bot (khác với webhook)
- **Kiểm tra tính hợp lệ của dữ liệu đầu vào**: Cả giao diện người dùng và máy chủ đều kiểm tra tính hợp lệ của dữ liệu đầu vào do người dùng nhập.
- **Bảo mật cơ sở dữ liệu**: Cơ sở dữ liệu SQLite cần được bảo vệ bằng các quyền truy cập tập tin phù hợp.
- **Bảo mật API**: Hãy cân nhắc triển khai giới hạn tốc độ truy cập trên các điểm cuối API của bạn.
- **CORS**: Cấu hình CORS đúng cách cho miền sản xuất của bạn.

## Ngành kiến trúc

```
Giao diện người dùng website ←→ API Express ←→ Bot Discord ←→ Máy chủ Discord
                    ↓
                Cơ sở dữ liệu SQLite
```

## Cấu trúc tệp

```
project/
├── src/components/DiscordComments/ # Thành phần giao diện người dùng
├── server/ # Máy chủ phụ trợ
│ ├── api.js # Máy chủ API Express
│ ├── discord-bot.js # Logic của bot Discord
│ ├── database.js # Các hàm cơ sở dữ liệu SQLite
│ └── package.json # Các phụ thuộc của máy chủ
└── docs/Discord/Docs/ # Tài liệu
```

## Hỗ trợ trình duyệt

Tính năng bình luận của Discord hoạt động trên tất cả các trình duyệt hiện đại hỗ trợ:

- API tìm nạp ES6
- React 18+
- CSS Grid và Flexbox
- Bộ nhớ cục bộ (để lưu trữ dữ liệu biểu mẫu)
