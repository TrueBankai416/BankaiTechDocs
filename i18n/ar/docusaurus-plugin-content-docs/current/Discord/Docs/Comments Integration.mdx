---
title: تكامل التعليقات
description: دليل شامل لدمج تعليقات ديسكورد ثنائية الاتجاه مع دوكوسورس، بما في ذلك الإعداد والتكوين واستكشاف الأخطاء وإصلاحها.
image: /img/social/discord-social.jpg
sidebar_position: 2
---

# دمج التعليقات على ديسكورد

يتضمن موقع التوثيق هذا **تكاملًا ثنائي الاتجاه لتعليقات Discord** يسمح للزوار بإرسال التعليقات إلى خادم Discord الخاص بك ويعرض ردود Discord مرة أخرى على الموقع الإلكتروني جنبًا إلى جنب مع تعليقات Giscus الموجودة على GitHub.

## Features

- **التواصل ثنائي الاتجاه**: تعليقات الموقع الإلكتروني ← ديسكورد، ردود ديسكورد ← الموقع الإلكتروني
- **نظام التعليقات المزدوج**: يعمل كل من نظام التعليقات Giscus (القائم على GitHub) ونظام التعليقات Discord معًا.
- **تضمينات ديسكورد الغنية**: تظهر التعليقات كعناصر مضمنة منسقة مع سياق الصفحة الكاملة
- **إدارة سلاسل المحادثات**: إنشاء سلاسل محادثات تلقائية لردود منظمة
- **تحديثات فورية**: يعرض الموقع الإلكتروني ردود ديسكورد تلقائيًا
- **سياق الصفحة الكاملة**: تتضمن التعليقات عنوان الصفحة، وعنوان URL، وسياق التنقل
- **صور المستخدمين**: تظهر صور المستخدمين وأسماء المستخدمين في ردود ديسكورد
- **تصميم متجاوب**: يعمل على كل من أجهزة الكمبيوتر المكتبية والأجهزة المحمولة

## تعليمات الإعداد

### 1. أنشئ بوت ديسكورد

1. انتقل إلى [بوابة مطوري ديسكورد](https://discord.com/developers/applications)
2. إنشاء تطبيق جديد
3. انتقل إلى قسم "البوت" وأنشئ بوتًا
4. انسخ رمز البوت
5. ضمن "نوايا البوابة المميزة"، قم بتمكين ما يلي:
   - **محتوى الرسالة والغرض منها**
   - **نية أعضاء الخادم** (اختياري)

### 7. نشر التكوين

يحتاج برنامج الروبوت الخاص بك إلى هذه الأذونات:

- قراءة الرسائل
- إرسال الرسائل
- Create Public Threads
- إرسال الرسائل في سلاسل المحادثات
- اقرأ سجل الرسائل
- استخدام أوامر الشرطة المائلة (اختياري)

### 5. تثبيت تبعيات الخادم

1. انتقل إلى OAuth2 > مُولِّد عناوين URL
2. حدد نطاق "البوت"
3. حدد الأذونات المذكورة أعلاه
4. استخدم الرابط الذي تم إنشاؤه لدعوة البوت إلى خادمك

### 4. Configure Environment Variables

#### الواجهة الأمامية (.env)

```env
# إعدادات بوت ديسكورد
DISCORD_BOT_TOKEN=your_bot_token_here
DISCORD_CHANNEL_ID=your_channel_id_here

# إعدادات واجهة برمجة التطبيقات
REACT_APP_API_URL=http://localhost:3001

# إعدادات سياق ديسكورد
DISCORD_CONTEXT_LEVEL=full
DISCORD_SHOW_BREADCRUMBS=true
DISCORD_SHOW_SECTION_INFO=true
اسم موقع ديسكورد = اسم موقعك
```

#### خادم الواجهة الخلفية (server/.env)

```env
# إعدادات بوت ديسكورد
DISCORD_BOT_TOKEN=your_bot_token_here
DISCORD_CHANNEL_ID=your_channel_id_here

# إعدادات واجهة برمجة التطبيقات
PORT=3001

# إعدادات سياق ديسكورد
DISCORD_CONTEXT_LEVEL=full
DISCORD_SHOW_PAGE_CONTEXT=true
DISCORD_SHOW_BREADCRUMBS=true
DISCORD_SHOW_SECTION_INFO=true
DISCORD_SHOW_PAGE_PATH=false
DISCORD_SHOW_TIMESTAMP=true
DISCORD_SHOW_AUTHOR_FIELD=true
DISCORD_CREATE_THREADS=true
DISCORD_THREAD_DURATION=1440
DISCORD_SITE_NAME=اسم موقعك
```

:::تحذير ملاحظة أمنية

- لا تقم أبدًا بإضافة ملفات `.env` إلى نظام التحكم في الإصدارات
- استخدم متغيرات البيئة في بيئة الإنتاج
- حافظ على أمان رمز البوت الخاص بك
  :::

### 6. Start the Backend Server

```bash
cd server
npm install
```

### 6. ابدأ تشغيل خادم الواجهة الخلفية

```bash
# التطوير
npm run dev

# الإنتاج
npm start
```

### 7. 7. Deploy Configuration

بالنسبة لعمليات النشر الإنتاجية، قم بتعيين متغيرات البيئة في منصة الاستضافة الخاصة بك:

- **الواجهة الأمامية**: قم بتعيين `REACT_APP_API_URL` إلى عنوان URL الخاص بواجهة برمجة التطبيقات (API) الخاصة بالإنتاج.
- **الخلفية**: انشر مجلد الخادم مع رمز البوت ومعرف القناة
- **قاعدة البيانات**: سيتم إنشاء قاعدة بيانات SQLite تلقائيًا
- **السياق**: اضبط مستوى سياق Discord وخياراته وفقًا لتفضيلاتك

## كيف يعمل

### تدفق ثنائي الاتجاه كامل

1. **تعليق على الموقع الإلكتروني**: يقوم المستخدم بإرسال تعليق عبر نموذج الموقع الإلكتروني
2. **رسالة ديسكورد**: يرسل البوت محتوى مضمّنًا منسقًا إلى قناة ديسكورد مع سياق الصفحة الكاملة
3. **إنشاء موضوع جديد**: يقوم البوت تلقائيًا بإنشاء موضوع جديد للردود المنظمة.
4. **ردود ديسكورد**: يرد مستخدمو ديسكورد في سلسلة المحادثات أو على الرسالة.
5. **تخزين قاعدة البيانات**: يقوم البوت بتخزين جميع التعليقات والردود في قاعدة بيانات SQLite
6. **عرض الموقع الإلكتروني**: يقوم واجهة المستخدم الأمامية باستطلاعات الرأي كل 30 ثانية وعرض ردود ديسكورد

### تنسيق رسالة ديسكورد

Discord embeds include:

- **Title**: "💬 New Comment: \{Page Title}"
- **الوصف**: محتوى التعليق الفعلي
- **المؤلف**: اسم المعلق
- **URL**: رابط قابل للنقر يؤدي إلى الصفحة المحددة
- **حقل الصفحة**: يعرض عنوان الصفحة مع رابط قابل للنقر
- **الطابع الزمني**: وقت نشر التعليق
- **موضوع**: موضوع تم إنشاؤه تلقائيًا للردود

### عرض الموقع الإلكتروني

يُظهر الموقع الإلكتروني ما يلي:

- **التعليقات الأصلية**: نماذج إرسال الموقع الإلكتروني
- **ردود ديسكورد**: ردود من مستخدمي ديسكورد مع صورهم الرمزية وأسماء المستخدمين
- **تحديثات فورية**: استطلاع تلقائي للردود الجديدة
- **المحادثات المتسلسلة**: هيكل منظم للتعليقات والردود

## تكوين السياق

### مستويات السياق

قم بضبط مقدار السياق الذي يظهر في رسائل ديسكورد:

#### `DISCORD_CONTEXT_LEVEL=minimal`

- **العروض**: عنوان الصفحة ومحتوى التعليق فقط
- **حالة الاستخدام**: رسائل واضحة وبسيطة بدون سياق إضافي

#### `DISCORD_CONTEXT_LEVEL=basic`

- **يعرض**: عنوان الصفحة، ومحتوى التعليقات، ورابط الصفحة القابل للنقر
- **حالة الاستخدام**: سياق معتدل مع معلومات أساسية

#### `DISCORD_CONTEXT_LEVEL=full` (افتراضي)

- **العروض**: جميع معلومات السياق بما في ذلك مسارات التنقل والأقسام والطوابع الزمنية
- **حالة الاستخدام**: توفير أقصى قدر من السياق لإجراء مناقشات تفصيلية

#### `DISCORD_CONTEXT_LEVEL=custom`

- **يعرض**: عناصر السياق التي تقوم بتمكينها بشكل فردي فقط
- **حالة الاستخدام**: تحكم دقيق في ما يظهر

### ضوابط السياق الفردي

عند استخدام `DISCORD_CONTEXT_LEVEL=custom`، يمكنك التحكم في كل عنصر:

```env
DISCORD_SHOW_PAGE_CONTEXT=true # عرض عنوان الصفحة وحقل عنوان URL
DISCORD_SHOW_BREADCRUMBS=true # عرض مسارات التنقل
DISCORD_SHOW_SECTION_INFO=true # عرض القسم الرئيسي (Docker، Nextcloud، إلخ.)
DISCORD_SHOW_PAGE_PATH=false # عرض مسار URL الكامل
DISCORD_SHOW_TIMESTAMP=true # عرض وقت نشر التعليق
DISCORD_SHOW_AUTHOR_FIELD=true # عرض اسم المعلق كمؤلف مضمن
```

### Thread Configuration

```env
DISCORD_CREATE_THREADS=true # إنشاء سلاسل محادثات للردود
DISCORD_THREAD_DURATION=1440 # مدة الأرشفة التلقائية لسلسلة المحادثات (بالدقائق)
DISCORD_SITE_NAME=اسم موقعك # اسم الموقع في التذييل
```

### مخرجات سياق المثال

**السياق الأدنى:**

```
دليل تثبيت Docker
"كان هذا الشرح مفيدًا حقًا! كيف يمكنني التعامل مع تعارضات المنافذ؟"
```

**السياق الأساسي:**

```
💬 دليل تثبيت Docker
"كان هذا الشرح مفيدًا جدًا! كيف أتعامل مع تعارضات المنافذ؟"
[رابط الصفحة]
```

**السياق الكامل:**

```
💬 New Comment: Docker Installation Guide
👤 John Doe
"This tutorial was really helpful! How do I handle port conflicts?"

📄 Page: Docker Installation Guide
📍 Navigation: Docker > Docs > Installing Docker
📂 Section: Docker
🔗 Path: /Docker/Docs/Installing%20Docker/Docker%20Engine

Your Site Name • Comment System
```

## التخصيص

### Styling

يستخدم مكون التعليقات في ديسكورد وحدات CSS. يمكنك تخصيص المظهر عن طريق تعديل ما يلي:

```
src/components/DiscordComments/styles.module.css
```

### تنسيق الرسالة

لتخصيص تنسيق تضمين Discord، قم بتحرير كائن `discordMessage` في:

```
src/components/DiscordComments/index.tsx
```

### وضع المكونات

تظهر تعليقات ديسكورد أسفل تعليقات جيسكوس بشكل افتراضي. لتغيير هذا، قم بتعديل ما يلي:

```
src/theme/DocItem/Layout/index.js
```

## استكشاف الأخطاء وإصلاحها

### لا يستجيب البوت

1. **تحقق من رمز البوت**: تأكد من صحة `DISCORD_BOT_TOKEN`
2. **التحقق من الأذونات**: تأكد من أن البوت لديه الأذونات المطلوبة في القناة
3. **تحقق من مُعرّف القناة**: تأكد من صحة `DISCORD_CHANNEL_ID`
4. **البوت موجود في الخادم**: تحقق من وجود البوت بالفعل في خادم Discord الخاص بك

### التعليقات لا تظهر

1. **الخادم قيد التشغيل**: تحقق مما إذا كان خادم الواجهة الخلفية يعمل على المنفذ الصحيح
2. **عنوان URL الخاص بواجهة برمجة التطبيقات**: تأكد من أن `REACT_APP_API_URL` يشير إلى خادم الواجهة الخلفية الخاص بك
3. **قاعدة البيانات**: تحقق مما إذا كانت قاعدة بيانات SQLite قيد الإنشاء في مجلد الخادم
4. **CORS**: تأكد من تهيئة CORS لنطاق الواجهة الأمامية الخاص بك

### لا تظهر الردود على ديسكورد

1. **هدف محتوى الرسالة**: تأكد من تفعيل هذا الخيار في بوابة مطوري ديسكورد.
2. **صلاحيات البوت**: تحقق من إمكانية قراءة البوت للرسائل في القناة
3. **اتصال قاعدة البيانات**: تحقق من سجلات الخادم بحثًا عن أخطاء في قاعدة البيانات
4. **استطلاعات الرأي**: يتم إجراء استطلاعات الرأي في واجهة المستخدم كل 30 ثانية - قد يستغرق ظهور الردود بعض الوقت

### متغيرات البيئة لا تعمل

- **التطوير**: أعد تشغيل كل من خوادم الواجهة الأمامية والخلفية
- **الإنتاج**: أعد نشر كل من الواجهة الأمامية والخلفية مع متغيرات البيئة الصحيحة
- **البادئة**: استخدم البادئة `REACT_APP_` فقط لمتغيرات الواجهة الأمامية

### مشاكل قاعدة البيانات

1. **أذونات الملفات**: تأكد من أن الخادم لديه صلاحية الكتابة إلى دليل قاعدة البيانات
2. **مساحة القرص**: تحقق من مساحة القرص المتاحة لقاعدة بيانات SQLite
3. **سجلات الخادم**: تحقق من وحدة تحكم الخادم بحثًا عن أخطاء في اتصال قاعدة البيانات

## الاعتبارات الأمنية

- **رمز البوت**: حافظ على سرية رمز البوت الخاص بك ولا تقم أبدًا بإضافته إلى نظام التحكم في الإصدارات.
- **Rate Limiting**: Discord has rate limits for bots (different from webhooks)
- **التحقق من صحة المدخلات**: يقوم كل من الواجهة الأمامية والخلفية بالتحقق من صحة مدخلات المستخدم.
- **أمان قاعدة البيانات**: يجب حماية قاعدة بيانات SQLite من خلال منح أذونات الملفات المناسبة.
- **أمان واجهة برمجة التطبيقات**: ضع في اعتبارك تطبيق تحديد معدل الاستخدام على نقاط نهاية واجهة برمجة التطبيقات الخاصة بك
- **CORS**: قم بتكوين CORS بشكل صحيح لنطاق الإنتاج الخاص بك

## Architecture

```
واجهة الموقع ←→ واجهة برمجة تطبيقات Express ←→ بوت Discord ←→ خادم Discord
                    ↓
                قاعدة بيانات SQLite
```

## بنية الملف

```
project/
├── src/components/DiscordComments/ # مكون الواجهة الأمامية
├── server/ # خادم الواجهة الخلفية
│ ├── api.js # خادم واجهة برمجة تطبيقات Express
│ ├── discord-bot.js # منطق بوت Discord
│ ├── database.js # وظائف قاعدة بيانات SQLite
│ └── package.json # تبعيات الخادم
└── docs/Discord/Docs/ # التوثيق
```

## دعم المتصفح

يعمل مكون التعليقات في ديسكورد في جميع المتصفحات الحديثة التي تدعم ما يلي:

- واجهة برمجة تطبيقات ES6 fetch
- تفاعل 18+
- شبكة CSS و Flexbox
- التخزين المحلي (للحفاظ على البيانات)
