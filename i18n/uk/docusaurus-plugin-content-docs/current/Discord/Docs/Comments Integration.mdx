---
title: Інтеграція коментарів
description: Повний посібник з інтеграції двонаправлених коментарів Discord з Docusaurus, включаючи налаштування, конфігурацію та усунення несправностей.
image: /img/social/discord-social.jpg
sidebar_position: 2
---

# Інтеграція коментарів Discord

Цей сайт документації містить **двонаправлену інтеграцію коментарів Discord**, яка дозволяє відвідувачам надсилати коментарі на ваш сервер Discord ТА відображати відповіді Discord на вебсайті разом із існуючими коментарями Giscus на базі GitHub.

## Особливості

* **Двонаправлений зв'язок**: Коментарі на сайті → Discord, відповіді на Discord → Сайт
* **Система подвійних коментарів**: Коментарі Giscus (на базі GitHub) та Discord працюють разом.
* **Розширені вбудовування Discord**: Коментарі відображаються як відформатовані вбудовування з повним контекстом сторінки
* **Керування темами**: Автоматичне створення тем для упорядкованих відповідей
* **Оновлення в режимі реального часу**: Вебсайт автоматично відображає відповіді Discord
* **Контекст повної сторінки**: Коментарі містять назву сторінки, URL-адресу та контекст навігації
* **Аватари користувачів**: У відповідях Discord відображаються аватари та імена користувачів
* **Адаптивний дизайн**: Працює як на настільних комп’ютерах, так і на мобільних пристроях

## Інструкції з налаштування

### 1. Створіть бота в Discord

1. Перейдіть на [Портал розробників Discord](https://discord.com/developers/applications)
2. Створити нову програму
3. Перейдіть до розділу «Бот» та створіть бота
4. Скопіюйте токен бота
5. У розділі «Наміри привілейованого шлюзу» увімкніть:
   * **Намір змісту повідомлення**
   * **Намір учасників сервера** (необов'язково)

### 2) Налаштування дозволів бота

Вашому боту потрібні такі дозволи:

* Читати повідомлення
* Надсилати повідомлення
* Створити публічні теми
* Надсилати повідомлення в темах
* Читати історію повідомлень
* Використання команд зі скльною рискою (необов'язково)

### 3. Запросити бота на сервер

1. Перейдіть до OAuth2 > Генератор URL-адрес
2. Виберіть область дії "бот"
3. Виберіть дозволи, перелічені вище
4. Використайте згенеровану URL-адресу, щоб запросити бота на ваш сервер

### 4) Налаштування змінних середовища

#### Фронтенд (.env)

```env
# Конфігурація бота Discord
DISCORD_BOT_TOKEN=тут_ваш_токен_бота
DISCORD_CHANNEL_ID=тут_ваш_канал_ідентифікатор

# Конфігурація API
REACT_APP_API_URL=http://localhost:3001

# Конфігурація контексту Discord
DISCORD_CONTEXT_LEVEL=повний
DISCORD_SHOW_BREADCRUMBS=true
DISCORD_SHOW_SECTION_INFO=true
DISCORD_SITE_NAME=Назва вашого сайту
```

#### Бекенд-сервер (server/.env)

```env
# Конфігурація бота Discord
DISCORD_BOT_TOKEN=тут_ваш_токен_бота
DISCORD_CHANNEL_ID=тут_ваш_канал_ідентифікатор

# Конфігурація API
PORT=3001

# Конфігурація контексту Discord
DISCORD_CONTEXT_LEVEL=повний
DISCORD_SHOW_PAGE_CONTEXT=true
DISCORD_SHOW_BREADCRUMBS=true
DISCORD_SHOW_SECTION_INFO=true
DISCORD_SHOW_PAGE_PATH=false
DISCORD_SHOW_TIMESTAMP=true
DISCORD_SHOW_AUTHOR_FIELD=true
DISCORD_CREATE_THREADS=true
DISCORD_THREAD_DURATION=1440
DISCORD_SITE_NAME=Назва вашого сайту
```

:::warning Примітка безпеки

* Ніколи не передавайте файли `.env` до системи контролю версій
* Використання змінних середовища у продакшені
* Зберігайте токен вашого бота в безпеці
  :::

### 5. Встановлення залежностей сервера

```bash
cd сервер
встановлення npm
```

### 6. Запустіть бекенд-сервер

```bash
# Розробка
npm run dev

# Виробництво
npm start
```

### 7. Розгортання конфігурації

Для розгортання у виробничому середовищі встановіть змінні середовища на вашій хостинговій платформі:

* **Фронтенд**: Встановіть `REACT_APP_API_URL` як URL-адресу вашого робочого API
* **Бекенд**: Розгорніть папку сервера з токеном бота та ідентифікатором каналу
* **База даних**: База даних SQLite буде створена автоматично
* **Контекст**: Налаштуйте рівень контексту та параметри Discord відповідно до ваших уподобань

## Як це працює

### Повний двонаправлений потік

1. **Коментар на веб-сайті**: Користувач надсилає коментар через форму на веб-сайті
2. **Повідомлення Discord**: Бот надсилає відформатований вбудовуваний елемент до каналу Discord з повним контекстом сторінки
3. **Створення тем**: Бот автоматично створює теми для впорядкованих відповідей
4. **Відповіді Discord**: Користувачі Discord відповідають у темі обговорення або на повідомлення
5. **Зберігання бази даних**: Бот зберігає всі коментарі та відповіді в базі даних SQLite
6. **Відображення веб-сайту**: Фронтенд опитує API кожні 30 секунд та відображає відповіді Discord

### Формат повідомлення Discord

Вбудовування Discord включає:

* **Title**: "💬 New Comment: \{Page Title}"
* **Опис**: Фактичний вміст коментаря
* **Автор**: Ім'я коментатора
* **URL**: Посилання, за яким можна перейти на потрібну сторінку
* **Поле сторінки**: Показує заголовок сторінки з посиланням, за яким можна натиснути
* **Мітка часу**: Дата публікації коментаря
* **Гілка**: Автоматично створена гілка для відповідей

### Відображення веб-сайту

На вебсайті показано:

* **Оригінальні коментарі**: Надсилання форм на вебсайт
* **Відповіді Discord**: Відповіді користувачів Discord з аватарами та іменами користувачів
* **Оновлення в режимі реального часу**: Автоматичне опитування щодо нових відповідей
* **Гістерезисні розмови**: Організований коментар → структура відповідей

## Конфігурація контексту

### Рівні контексту

Налаштуйте, скільки контексту відображатиметься в повідомленнях Discord:

#### `DISCORD_CONTEXT_LEVEL=мінімальний`

* **Показує**: Тільки заголовок сторінки та вміст коментарів
* **Випадок використання**: Прості, зрозумілі повідомлення без зайвого контексту

#### `DISCORD_CONTEXT_LEVEL=базовий`

* **Показує**: Заголовок сторінки, вміст коментарів та URL-адресу сторінки, на яку можна натискати
* **Варіянт використання**: Помірний контекст із важливою інформацією

#### `DISCORD_CONTEXT_LEVEL=повний` (за замовчуванням)

* **Показує**: Уся контекстна інформація, включаючи навігаційні хащі, розділи, позначки часу
* **Випадок використання**: Максимальний контекст для детальних обговорень

#### `DISCORD_CONTEXT_LEVEL=користувацький`

* **Показує**: Тільки ті елементи контексту, які ви вмикаєте окремо
* **Випадок використання**: Точний контроль над тим, що відображається

### Окремі елементи керування контекстом

Використовуючи `DISCORD_CONTEXT_LEVEL=custom`, ви можете контролювати кожен елемент:

```env
DISCORD_SHOW_PAGE_CONTEXT=true # Показати поле заголовка сторінки та URL
DISCORD_SHOW_BREADCRUMBS=true # Показати навігаційні хлібні крихти
DISCORD_SHOW_SECTION_INFO=true # Показати основний розділ (Docker, Nextcloud тощо)
DISCORD_SHOW_PAGE_PATH=false # Показати повний URL-шлях
DISCORD_SHOW_TIMESTAMP=true # Показати, коли коментар було опубліковано
DISCORD_SHOW_AUTHOR_FIELD=true # Показати ім'я коментатора як автора вбудовування
```

### Конфігурація потоку

```env
DISCORD_CREATE_THREADS=true # Створити теми для відповідей
DISCORD_THREAD_DURATION=1440 # Час автоматичного архівування теми (хвилини)
DISCORD_SITE_NAME=Назва вашого сайту # Назва сайту в нижньому колонтитулі
```

### Приклади контекстних виходів

**Мінімальний контекст:**

```
Посібник зі встановлення Docker
"Цей посібник був справді корисним! Як мені вирішити конфлікти портів?"
```

**Основний контекст:**

```
💬 Посібник зі встановлення Docker
"Цей посібник був справді корисним! Як мені вирішити конфлікти портів?"
[Посилання на сторінку]
```

**Повний контекст:**

```
💬 Новий коментар: Посібник зі встановлення Docker
👤 Джон Доу
"Цей посібник був справді корисним! Як мені вирішити конфлікти портів?"

📄 Сторінка: Посібник зі встановлення Docker
📍 Навігація: Docker > Документація > Встановлення Docker
📂 Розділ: Docker
🔗 Шлях: /Docker/Документація/Встановлення%20Docker/Docker%20Eengine

Назва вашого сайту • Система коментарів
```

## Налаштування

### Стайлінг

Компонент коментарів Discord використовує CSS-модулі. Ви можете налаштувати зовнішній вигляд, змінивши:

```
src/components/DiscordComments/styles.module.css
```

### Формат повідомлення

Щоб налаштувати формат вбудовування Discord, відредагуйте об'єкт `discordMessage` у:

```
src/components/DiscordComments/index.tsx
```

### Розміщення компонентів

Коментарі Discord за замовчуванням відображаються під коментарями Giscus. Щоб змінити це, змініть:

```
src/theme/DocItem/Layout/index.js
```

## Усунення несправностей

### Бот не відповідає

1. **Перевірте токен бота**: переконайтеся, що `DISCORD_BOT_TOKEN` правильний
2. **Перевірка дозволів**: Переконайтеся, що бот має необхідні дозволи в каналі
3. **Перевірте ідентифікатор каналу**: переконайтеся, що `DISCORD_CHANNEL_ID` правильний
4. **Бот на сервері**: Перевірте, чи бот справді знаходиться на вашому сервері Discord

### Коментарі не відображаються

1. **Сервер працює**: Перевірте, чи сервер сервера працює на правильному порту
2. **URL API**: Перевірте, чи вказує `REACT_APP_API_URL` на ваш сервер-бекенд
3. **База даних**: Перевірка, чи створюється база даних SQLite у папці сервера
4. **CORS**: Переконайтеся, що CORS налаштовано для вашого фронтенд-домену

### Відповіді Discord не відображаються

1. **Намір щодо вмісту повідомлення**: Переконайтеся, що це ввімкнено на порталі розробників Discord
2. **Дозволи бота**: Перевірте, чи може бот читати повідомлення в каналі
3. **Підключення до бази даних**: Перевірте журнали сервера на наявність помилок бази даних
4. **Опитування**: Фронтенд проводить опитування кожні 30 секунд – відповіді можуть з’являтися пізніше

### Змінні середовища не працюють

* **Розробка**: Перезапустіть обидва фронтенд-сервери та сервери бекенду
* **Продакшн**: Перерозгорніть фронтенд та бекенд з правильними змінними середовища
* **Префікс**: Використовуйте префікс `REACT_APP_` лише для змінних інтерфейсу

### Проблеми з базою даних

1. **Дозволи на запис файлів**: Переконайтеся, що сервер може записувати дані до каталогу бази даних
2. **Місце на диску**: Перевірте доступне місце на диску для бази даних SQLite
3. **Журнали сервера**: Перевірте консоль сервера на наявність помилок підключення до бази даних

## Міркування безпеки

* **Токен бота**: Зберігайте свій токен бота в таємниці та ніколи не передавайте його в систему контролю версій.
* **Обмеження швидкості**: Discord має обмеження швидкості для ботів (відмінні від вебхуків)
* **Перевірка вводу**: Як фронтенд, так і бекенд перевіряють введені користувачем дані.
* **Безпека бази даних**: Базу даних SQLite слід захистити належними дозволами на доступ до файлів.
* **Безпека API**: Розгляньте можливість впровадження обмеження швидкості на кінцевих точках API
* **CORS**: Правильно налаштуйте CORS для вашого робочого домену

## Архітектура

```
Фронтенд веб-сайту ←→ Express API ←→ Discord Bot ←→ Discord Server
                    ↓
                База даних SQLite
```

## Структура файлу

```
project/
├── src/components/DiscordComments/ # Фронтенд-компонент
├── server/ # Бекенд-сервер
│ ├── api.js # Сервер Express API
│ ├── discord-bot.js # Логіка бота Discord
│ ├── database.js # Функції бази даних SQLite
│ └── package.json # Залежності сервера
└── docs/Discord/Docs/ # Документація
```

## Підтримка браузерів

Компонент коментарів Discord працює у всіх сучасних браузерах, які підтримують:

* API вилучення ES6
* Реакція 18+
* CSS Grid та Flexbox
* Локальне сховище (для збереження форми)
